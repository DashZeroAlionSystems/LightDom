declare module '@tensorflow/tfjs-node' {
  // Minimal tfjs-node stub for build-time only. Replace with proper types
  // or install @types/tensorflow if you add runtime TF code.
  const tf: any;
  export = tf;
}
