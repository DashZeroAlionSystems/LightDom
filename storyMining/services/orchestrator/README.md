# Orchestrator Service

Coordinates StoryMiner jobs (discovery → crawl → classify → enrich) using the shared message bus. Designed to run as a standalone worker or scale horizontally via queue consumers.
