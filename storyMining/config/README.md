# Configuration

Store crawler discovery rules, deployment manifests, and environment templates here. Each environment should provide a `*.env.example` file plus JSON/YAML configs referenced by the orchestrator.
