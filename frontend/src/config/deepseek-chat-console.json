{
  "templates": [
    {
      "id": "workflow-plan",
      "label": "Workflow Planner",
      "badge": "automation",
      "description": "Step-by-step execution plan with linked services and schema calls.",
      "default": true
    },
    {
      "id": "component-kit",
      "label": "Component Kit",
      "badge": "design system",
      "description": "React component blueprint with MD3 tokens and variation hints."
    },
    {
      "id": "prompt-critique",
      "label": "Prompt Critique",
      "badge": "analysis",
      "description": "Review original prompt, annotate gaps, and suggest schema defaults."
    }
  ],
  "admin": {
    "accentTone": "primary",
    "highlightMode": "balanced",
    "showActionButtons": true
  },
  "tools": [
    {
      "id": "workflow-registry-lookup",
      "modelIds": ["llama3.1", "qwen3"],
      "function": {
        "name": "workflowRegistryLookup",
        "description": "Fetch workflow metadata and schema definitions for a given workflow id.",
        "parameters": {
          "type": "object",
          "properties": {
            "workflowId": {
              "type": "string",
              "description": "Identifier of the workflow or bundle."
            }
          },
          "required": ["workflowId"]
        }
      },
      "autoExecute": true
    },
    {
      "id": "design-system-palette",
      "modelIds": ["llama3.1", "command-r-plus"],
      "function": {
        "name": "designSystemPalette",
        "description": "Return MD3 tokens and component variants for a given template id.",
        "parameters": {
          "type": "object",
          "properties": {
            "templateId": {
              "type": "string",
              "description": "Template identifier requested by DeepSeek."
            },
            "includeExamples": {
              "type": "boolean",
              "description": "Whether to include JSX snippets in response.",
              "default": false
            }
          },
          "required": ["templateId"]
        }
      },
      "autoExecute": false
    }
  ],
  "stories": {
    "deepseek-console/full": {
      "styleGuide": "../style-guides/deepseek-chat.json",
      "prompt": "../stories/prompts/deepseek-console.mdx",
      "fixture": "../stories/fixtures/deepseek-console-session.json"
    }
  }
}
